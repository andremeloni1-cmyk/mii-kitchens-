{
  "name": "mii-kitchens-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Mii Kitchens Hub — internal org dashboard (jobs, drafters, installers, meetings, maintenance). Node/Express + MySQL, Google Calendar sync via Apps Script.",
  "type": "commonjs",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
